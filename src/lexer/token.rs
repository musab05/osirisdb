use crate::lexer::spanned_token::Span;

#[derive(Debug, Clone, PartialEq)]
pub enum Modifier {
    Temporary,
    Temp,
    Unlogged,
    Global,
    Local,
    Materialized,
    Replace,
}

#[derive(Debug, Clone, PartialEq)]
pub enum TokenKind {
    // DML
    Select,
    From,
    Where,
    Insert,
    Into,
    Values,
    Update,
    Set,
    Delete,
    Returning,
    Merge,
    // DDL
    Create,
    Table,
    Tables,
    Schema,
    Authorization,
    Drop,
    Truncate,
    Alter,
    Reset,
    Restart,
    Identity,
    Continue,
    Index,
    View,
    Add,
    Attach,
    Column,
    Rename,
    To,
    Owner,
    Type,
    Statistics,
    Storage,
    Options,
    Data,
    // Constraints
    Constraint,
    Primary,
    Key,
    Foreign,
    References,
    Unique,
    Check,
    Not,
    Null,
    Default,
    Cascade,
    Restrict,
    Action,
    // Logical
    And,
    Or,
    In,
    Like,
    Ilike,
    Similar,
    Between,
    Is,
    Exists,
    Any,
    Some,
    Escape,
    // Boolean
    True,
    False,
    // Grouping / Ordering
    Group,
    Order,
    By,
    Having,
    Limit,
    Offset,
    Asc,
    Desc,
    Distinct,
    All,
    Nulls,
    First,
    Last,
    Fetch,
    Next,
    PercentKw,
    Tie,
    Ties,
    // Set ops
    Union,
    Intersect,
    Except,
    // Transaction
    Begin,
    Commit,
    Rollback,
    Transaction,
    Savepoint,
    Release,
    // CTE / Conditional
    With,
    Recursive,
    Case,
    When,
    Then,
    Else,
    End,
    Cast,
    If,
    // Joins
    Join,
    On,
    As,
    Inner,
    Left,
    Right,
    Full,
    Cross,
    Outer,
    Natural,
    Using,
    Lateral,
    // Table options
    Inherits,
    Inherit,
    Partition,
    Range,
    List,
    Hash,
    Tablespace,
    Collate,
    Generated,
    Always,
    Stored,
    AutoIncrement,
    Detach,
    Minvalue,
    Maxvalue,
    // ON COMMIT
    Preserve,
    Rows,
    // Utility
    Explain,
    Analyze,
    Describe,
    Show,
    Copy,
    Vacuum,
    // Locking
    For,
    Share,
    UpdateKw,
    No,
    Wait,
    Skip,
    Locked,
    Only,
    // Window
    Over,
    Filter,
    Window,
    RangeKw,
    Preceding,
    Following,
    Current,
    Row,
    Unbounded,
    // Upsert
    Conflict,
    Do,
    Nothing,
    Excluded,
    // Data types
    Varying,
    Precision,
    Zone,
    Time,
    // Modifiers
    Modifier(Modifier),

    // Identifiers — zero-copy, resolved via span
    Ident,
    QuotedIdent,

    // Literals
    IntLit(i64),
    FloatLit(f64),
    StringLit, // content resolved via span (or post-processed string for escapes)
    BitStringLit,
    HexStringLit,
    ByteaLit,
    DollarStringLit,
    Parameter(u32),

    // Operators — Comparison
    Eq,
    Ne,
    Lt,
    Le,
    Gt,
    Ge,
    // Operators — Arithmetic
    Plus,
    Minus,
    Star,
    Slash,
    Percent,
    // Operators — String/JSON/Cast
    Concat,
    Arrow,
    DoubleArrow,
    DoubleColon,
    MinusGt,
    // Operators — Bitwise
    Pipe,
    Caret,
    Ampersand,
    Tilde,
    ShiftLeft,
    ShiftRight,
    // Operators — Regex
    RegexMatch,
    RegexIMatch,
    RegexNotMatch,
    RegexNotIMatch,
    // Operators — JSONB
    At,
    AtGt,
    LtAt,
    AtAt,
    HashToken,
    HashArrow,
    HashDoubleArrow,
    HashMinus,
    Question,
    QuestionPipe,
    QuestionAmp,
    // Punctuation
    LParen,
    RParen,
    LBracket,
    RBracket,
    LBrace,
    RBrace,
    Comma,
    Semicolon,
    Dot,

    // EOF
    Eof,

    // Errors — span holds position, payload holds the bad byte
    Illegal(u8),
    UnexpectedChar(u8),
    UnterminatedString,
    UnterminatedComment,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Token {
    pub kind: TokenKind,
    pub span: Span,
}
